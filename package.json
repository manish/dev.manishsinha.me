{
  "name": "dev-manishsinha-me",
  "version": "1.0.0",
  "description": "Personal development website",
  "scripts": {
    "build": "npm run build:sqlite-otel",
    "build:sqlite-otel": "rm -rf dist/sqlite-otel && mkdir -p dist/sqlite-otel && cp -r sqlite-otel/site/* dist/sqlite-otel/ && echo 'Build complete. Files copied:' && ls -la dist/sqlite-otel/index.html",
    "dev": "npm run build && npx serve dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "dependencies": {
    "wrangler": "^4.20.5"
  }
}
